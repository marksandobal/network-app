<div class="container">
  <h2><%= t('.products') %></h2>
  <div class="list-group">
    <% products.each do |p| %>
    <%= link_to "#{p.name} - #{p.price}", product_path(p), class: "list-group-item list-group-item-action" %>
    <% end %>
  </div>
</div>